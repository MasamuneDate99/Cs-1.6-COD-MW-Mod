#if defined _premiump_included
        #endinput
#endif

#define _premiump_included

#pragma library "premiump"

native premium_points_add(id, ile) // Zmiena Premium Pointsy na wartoϾ ile
native premium_points_get(id) // Pobiera Premium Points'y
native premium_is_vip(id) // Sprawdza czy gracz jest vipem

